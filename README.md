# myAppartment

```bash
$ keytool.exe -genkey -v -keystore my-appartment.keystore -alias my_appartment -keyalg RSA -keysize 2048 -validity 10000

Generating 2,048 bit RSA key pair and self-signed certificate (SHA256withRSA) with a validity of 10,000 days
        for: CN=KUMAIL RAZA, OU=Development, O=HzTech, L=Karachi, ST=Sindh, C=PK
```